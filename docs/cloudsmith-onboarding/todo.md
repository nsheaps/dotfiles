# Todos

## Org onboarding

- [ ] Onboard to Cloudsmith (org policy requires it when public package indexes are used during dependency installs) — https://ouraring.atlassian.net/wiki/spaces/SW/pages/429498723/Cloudsmith
  - Trigger: org-ops-nsheaps used `npx github-label-sync` (public npm registry) for a one-off local dry-run verification while fixing nsheaps/.github#177 — no persistent install/commit, but flagged per policy.
